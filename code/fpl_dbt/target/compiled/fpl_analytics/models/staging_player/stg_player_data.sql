

WITH player_data AS (
    SELECT * FROM `fpl-analytics-488811`.`raw_player`.`raw_player_data`
),

stg_player_data AS (
    SELECT
        id AS player_id,
        code AS pl_id,
        element_type,
        first_name,
        form,
        news,
        points_per_game,
        second_name,
        status,
        team AS team_id,
        total_points,
        web_name,
        known_name,
        minutes,
        goals_scored,
        assists,
        clean_sheets,
        goals_conceded,
        own_goals,
        penalties_saved,
        penalties_missed,
        yellow_cards,
        red_cards,
        saves,
        bonus,
        bps,
        influence,
        creativity,
        threat,
        ict_index,
        clearances_blocks_interceptions,
        recoveries,
        tackles,
        defensive_contribution,
        starts,
        expected_goals,
        expected_assists,
        expected_goal_involvements,
        expected_goals_conceded,
        corners_and_indirect_freekicks_order,
        corners_and_indirect_freekicks_text,
        direct_freekicks_order,
        direct_freekicks_text,
        penalties_order,
        penalties_text,
        influence_rank,
        influence_rank_type,
        creativity_rank,
        creativity_rank_type,
        threat_rank,
        threat_rank_type,
        ict_index_rank,
        ict_index_rank_type,
        expected_goals_per_90,
        saves_per_90,
        expected_assists_per_90,
        expected_goal_involvements_per_90,
        expected_goals_conceded_per_90,
        goals_conceded_per_90,
        form_rank,
        form_rank_type,
        points_per_game_rank,
        points_per_game_rank_type,
        starts_per_90,
        clean_sheets_per_90,
        defensive_contribution_per_90
    FROM player_data
)

SELECT * FROM stg_player_data